# docker

Docker image builds.

## Build Docker Image

```bash
make name={image-name} version={image-version} build
```

## Push Docker Image

```bash
make name={image-name} version={image-version} push
```

